{
  "name": "practice-two",
  "version": "1.0.0",
  "description": "- Apply knowledge to responsive design - Apply Grid css - Used media queries for popular screen size",
  "scripts": {
    "start": "rm -rf dist && rm -rf .parcel-cache && parcel src/index.html --no-cache --no-source-maps",
    "build": "rm -rf build && rm -rf .parcel-cache && parcel build ./src/*.html --no-cache --no-source-maps --dist-dir build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "parcel": "^2.9.3"
  },
  "devDependencies": {}
}
