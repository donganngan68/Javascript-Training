{
  "name": "big-practice",
  "version": "1.0.0",
  "description": "List customer, add/edit a customer, remove a customer",
  "scripts": {
    "start": "parcel src/index.html",
    "build": "rm -rf build && rm -rf .parcel-cache && parcel build ./src/*.html --no-cache --no-source-maps --dist-dir build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "parcel": "^2.9.3"
  },
  "devDependencies": {
    "eslint": "^8.45.0"
  }
}
